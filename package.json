{
  "name": "kjmlaw",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "googleapis": "^23.0.0"
  },
  "devDependencies": {
    "mustache": "^2.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-html": "node templating/generate-html.js",
    "pull-generate-push": "git pull && npm run generate-html && git add *.html && git commit --allow-empty -m 'Webhook update' && git push && git checkout master && git checkout development about.html contact.html locations.html services.html index.html && git commit --allow-empty -m 'Webhook update' && git push"
  },
  "author": "Tyler Johnson <mail@tyler-johnson.ca> (http://github.com/tejohnso)",
  "license": "ISC"
}
